trigger:
  branches:
    include:
      - master

pr:
  branches:
    include:
      - master

pool:
  vmImage: 'ubuntu-22.04'

steps:
- checkout: self
  clean: true
  displayName: 'Checkout latest commit'

- script: |
    echo "Building commit: $(Build.SourceVersion)"
    git log -1 --pretty=format:"%h - %s (%an, %ar)"
  displayName: 'Show commit info'

- task: UseDotNet@2
  displayName: 'Setup .NET'
  inputs:
    packageType: 'sdk'
    version: '10.0.x'
    installationPath: $(Agent.ToolsDirectory)/dotnet

- script: dotnet restore
  displayName: 'Restore dependencies'

- script: dotnet build --no-restore
  displayName: 'Build'

- script: |
    pwsh Playwright001/bin/Debug/net10.0/playwright.ps1 install --with-deps
  displayName: 'Install Playwright browsers'

- script: |
    ls -la Playwright001/bin/Debug/net10.0/
    pwsh Playwright001/bin/Debug/net10.0/playwright.ps1 --version
  displayName: 'Verify Playwright installation'

- script: |
    dotnet test --no-build --verbosity normal \
      --logger "trx;LogFileName=test_results.trx" \
      --logger "console;verbosity=detailed"
  displayName: 'Run Playwright Tests'
  continueOnError: true

# Publish structured TRX results to Tests tab
- task: PublishTestResults@2
  displayName: 'Publish Test Results to Pipeline'
  condition: succeededOrFailed()
  inputs:
    testResultsFiles: '**/test_results.trx'
    testResultsFormat: 'VSTest'
    failTaskOnFailedTests: false

# Publish HTML report as artifact
- task: PublishPipelineArtifact@1
  displayName: 'Publish HTML Test Report'
  condition: succeededOrFailed()
  inputs:
    targetPath: 'Playwright001/Reports/AutomationReport.html'
    artifact: 'AutomationReport'
    publishLocation: 'pipeline'

# Copy failure screenshots if tests fail
- task: CopyFiles@2
  displayName: 'Copy failure screenshots to staging'
  condition: failed()
  inputs:
    SourceFolder: '$(Build.SourcesDirectory)'
    Contents: '**/failure-*.png'
    TargetFolder: '$(Build.ArtifactStagingDirectory)/screenshots'
    flattenFolders: true

- task: PublishPipelineArtifact@1
  displayName: 'Publish failure screenshots'
  condition: failed()
  inputs:
    targetPath: '$(Build.ArtifactStagingDirectory)/screenshots'
    artifactName: 'playwright-screenshots'
    publishLocation: 'pipeline'